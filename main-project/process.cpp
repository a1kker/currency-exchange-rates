#include "processing.h"

int process() {
    // –еализаци€: например, вычисление максимальной разницы
    // между курсами покупки и продажи
    // (это просто пример, вам нужно будет адаптировать под ваши данные)
    double maxDifference = 0.0; // «десь вы должны реализовать логику
    // Ќапример, если у вас есть массив курсов, вы можете пройтись по нему и найти максимальную разницу
    return static_cast<int>(maxDifference);
}